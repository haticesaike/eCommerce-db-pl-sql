select*from table(sorted_by_price_asc);
select*from table(sorted_by_price_desc);


select*from table(sorted_by_filter);
select*from table(sorted_by_filter(415));

select*from table(sorted_by_purchase_desc);
select*from table(sorted_by_purchase_asc);

